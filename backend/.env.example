# Server
PORT=3000
NODE_ENV=development

# Feature flags (mock vs real)
USE_MOCK_LLM=true
USE_MOCK_DB=true

# OpenAI (Phase 5 — leave empty for mock mode)
OPENAI_API_KEY=

# PostgreSQL (Phase 5 — leave empty for mock mode)
DATABASE_URL=

# Redis (Phase 6 — leave empty for mock mode)
REDIS_URL=
